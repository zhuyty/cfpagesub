# Mock values for local development
# These are not real credentials, just placeholders
# The KV functions will use the in-memory fallback

# Uncomment these when you have real credentials
# KV_REST_API_URL=https://your-kv-project.kv.vercel-storage.com
# KV_REST_API_TOKEN=your-token-here

# GitHub VFS fallback configuration
VFS_GITHUB_OWNER=lonelam
VFS_GITHUB_REPO=subconverter-rs
VFS_GITHUB_BRANCH=main
VFS_GITHUB_ROOT_PATH=base 