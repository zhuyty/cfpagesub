{
    "version": 2,
    "routes": [
        {
            "src": "/api/admin/(.*)",
            "dest": "/api/admin/[...path].ts"
        },
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        }
    ]
}